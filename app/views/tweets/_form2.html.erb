<%= form_for @tweet do |form| %>
  <div>
    <%= form.text_area :text, class: 'form-control', maxlength: 140, required: true %>
    <div style='font-size: 9.5px'>
      *140 character limit
    </div>
    <%= form.submit 'Update Tweet', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
