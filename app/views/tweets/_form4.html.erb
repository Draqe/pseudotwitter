<%= form_for @tweet, url: user_reply_path(@tweet) do |form| %>
  <div>
    <%= form.text_area :text, class: 'form-control' %>
    <%= form.submit 'Update Reply', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
